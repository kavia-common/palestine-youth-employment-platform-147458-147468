# Core API stack (stable, wheel-friendly)
fastapi~=0.110
uvicorn[standard]~=0.23
starlette~=0.36
anyio~=4.3
h11~=0.14

# Validation and settings
pydantic~=2.6
pydantic-settings~=2.2
python-dotenv~=1.0

# Data and DB
SQLAlchemy~=2.0
psycopg[binary]~=3.1

# HTTP client
httpx~=0.24

# Auth and security
# Prefer python-jose without cryptography extra to avoid native builds in constrained envs
python-jose~=3.3
# Use passlib without bcrypt to avoid native bcrypt builds; pbkdf2_sha256 works out of the box
passlib==1.7.4

# Form parsing
python-multipart~=0.0.9

# Supabase client
supabase~=2.4

# Logging
loguru~=0.7

# Optional dev tools (kept minimal to reduce build size/issues)
# pytest~=8.0
# flake8~=7.0
